[build]
builder = "NIXPACKS"
buildCommand = "npm install && npx prisma generate"

[deploy]
startCommand = "npx prisma migrate deploy && node prisma/seed.js && node index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
